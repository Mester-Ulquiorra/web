{
  "name": "ulquiorra-gui",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "astro": "astro",
    "preview": "wrangler pages dev ./dist",
    "update": "rm -rf node_modules && rm -f bun.lockb && ncu -u && bun install"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^6.8.0",
    "astro": "^2.10.7",
    "nanostores": "^0.9.3",
    "scss": "^0.2.4"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "wrangler": "^3.5.0"
  }
}